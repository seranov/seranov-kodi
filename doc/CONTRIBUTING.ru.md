# Участие в разработке репозитория аддонов Seranov для Kodi

Спасибо за интерес к участию в проекте! Это руководство поможет вам создать новые плагины-службы для этого репозитория.

## Создание нового плагина-службы

### Шаг 1: Используйте NFO Scanner в качестве примера

Аддон `service.seranov.nfoscanner` представляет собой хороший пример полного плагина-службы с:

- Фоновым сканированием с настраиваемыми интервалами
- Мониторингом событий (воспроизведение, изменения настроек)
- Интеграцией JSON-RPC API с Kodi
- Поддержкой потоков
- Управлением настройками
- Поддержкой локализации

Вы можете использовать его в качестве примера для создания собственных плагинов-служб.

### Шаг 2: Создайте структуру своей службы

```bash
# Создайте директорию службы
mkdir -p service.seranov.yourplugin/resources/lib
mkdir -p service.seranov.yourplugin/resources/language/resource.language.en_gb
```

### Шаг 3: Обновите addon.xml

Отредактируйте `service.seranov.yourplugin/addon.xml`:

1. Измените атрибут `id`: `service.seranov.yourplugin`
2. Обновите атрибут `name`: `Имя вашего плагина`
3. Обновите `version` на `1.0.0`
4. Обновите `summary` и `description`
5. Оставьте `provider-name` как `seranov` или измените на ваше имя

Пример:
```xml
<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<addon id="service.seranov.myplugin" name="Моя пользовательская служба" version="1.0.0" provider-name="seranov">
    <requires>
        <import addon="xbmc.python" version="3.0.0"/>
    </requires>
    <extension point="xbmc.service" library="service.py"/>
    <extension point="xbmc.addon.metadata">
        <summary lang="ru_RU">Мой плагин-служба</summary>
        <description lang="ru_RU">Эта служба делает что-то полезное</description>
        ...
    </extension>
</addon>
```

### Шаг 4: Реализуйте логику вашей службы

Отредактируйте `service.seranov.yourplugin/service.py`:

**Для шаблона 1 (периодическая служба):**
- Измените метод `do_service_work()` для реализации вашей логики
- Настройте параметр `check_interval` по необходимости

**Для шаблона 2 (служба, управляемая событиями):**
- Реализуйте методы `on_playback_started()` и `on_playback_stopped()`
- Добавьте дополнительные обработчики событий в класс `ServiceMonitor`
- Измените метод `run()` при необходимости

### Шаг 5: Обновите настройки

Отредактируйте `service.seranov.yourplugin/resources/settings.xml`:

Добавьте или измените настройки для вашего плагина. Пример:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<settings>
    <category label="32001">
        <setting id="my_setting" type="text" label="32020" default="значение"/>
        <setting id="enable_feature" type="bool" label="32021" default="true"/>
    </category>
</settings>
```

### Шаг 6: Обновите языковые строки

Отредактируйте `service.seranov.yourplugin/resources/language/resource.language.en_gb/strings.xml`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<strings>
    <string id="32000">Имя вашего плагина</string>
    <string id="32001">Общие</string>
    <string id="32020">Моя настройка</string>
    <string id="32021">Включить функцию</string>
</strings>
```

Для русской локализации создайте файл `resources/language/resource.language.ru_ru/strings.xml`:

```xml
<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<strings>
    <string id="32000">Имя вашего плагина</string>
    <string id="32001">Общие</string>
    <string id="32020">Моя настройка</string>
    <string id="32021">Включить функцию</string>
</strings>
```

### Шаг 7: Добавьте свою иконку

Замените иконку при желании:
- Файл: `service.seranov.yourplugin/icon.png`
- Рекомендуемый размер: 256x256 пикселей или 512x512 пикселей
- Формат: PNG с прозрачностью

### Шаг 8: Тестируйте ваш плагин локально

1. Скопируйте директорию вашего плагина в папку аддонов Kodi:
   - Linux: `~/.kodi/addons/`
   - Windows: `%APPDATA%\Kodi\addons\`
   - macOS: `~/Library/Application Support/Kodi/addons/`

2. Перезапустите Kodi или обновите аддоны

3. Включите вашу службу в **Настройки** → **Дополнения** → **Мои дополнения** → **Службы**

4. Проверьте лог Kodi на наличие ошибок:
   - Linux/macOS: `~/.kodi/temp/kodi.log`
   - Windows: `%APPDATA%\Kodi\kodi.log`

### Шаг 9: Сгенерируйте файлы репозитория

Запустите скрипт генерации репозитория:

```bash
python3 scripts/generate_repo.py
```

Это выполнит:
- Создание ZIP-файла для вашего плагина в директории `repo/`
- Обновление `addons.xml` с метаданными вашего плагина
- Обновление контрольной суммы `addons.xml.md5`

### Шаг 10: Отправьте ваш плагин

1. Зафиксируйте ваши изменения:
   ```bash
   git add service.seranov.yourplugin/
   git commit -m "Add service.seranov.yourplugin"
   ```

2. Отправьте в вашу ветку и создайте pull request

## Лучшие практики

### Качество кода

- Используйте понятные имена переменных и функций
- Добавляйте комментарии для сложной логики
- Следуйте руководству по стилю PEP 8 для кода Python
- Обрабатывайте исключения корректно
- Используйте логирование вместо print

### Настройки

- Предоставляйте разумные значения по умолчанию
- Группируйте связанные настройки в категории
- Используйте подходящие типы настроек (text, number, bool, select)
- Добавляйте валидацию пользовательского ввода

### Логирование

Всегда используйте систему логирования Kodi:

```python
import xbmc

xbmc.log('[service.seranov.yourplugin] Ваше сообщение', xbmc.LOGINFO)
```

Уровни логирования:
- `xbmc.LOGDEBUG`: Отладочные сообщения
- `xbmc.LOGINFO`: Информационные сообщения
- `xbmc.LOGWARNING`: Предупреждения
- `xbmc.LOGERROR`: Ошибки
- `xbmc.LOGFATAL`: Критические ошибки

### Производительность

- Используйте `monitor.waitForAbort()` для задержек вместо `time.sleep()`
- Не выполняйте тяжелые операции в основном потоке
- Учитывайте флаг `abortRequested()` для чистого завершения
- Следите за использованием ресурсов (CPU, память, сеть)

### Локализация

- Используйте ID строк (32000+) для всего пользовательского текста
- Как минимум предоставьте английские строки
- Рассмотрите возможность добавления переводов на другие языки

## Распространенные шаблоны

### Чтение настроек

```python
import xbmcaddon

addon = xbmcaddon.Addon()
my_setting = addon.getSetting('my_setting')
enable_feature = addon.getSetting('enable_feature') == 'true'
```

### Показ уведомлений

```python
import xbmcgui

xbmcgui.Dialog().notification(
    heading='Мой плагин',
    message='Что-то произошло',
    icon=xbmcgui.NOTIFICATION_INFO,
    time=5000  # миллисекунды
)
```

### Выполнение команд JSON-RPC

```python
import xbmc
import json

request = {
    'jsonrpc': '2.0',
    'method': 'Player.GetActivePlayers',
    'id': 1
}
response = json.loads(xbmc.executeJSONRPC(json.dumps(request)))
```

## Ресурсы

- [Разработка аддонов для Kodi](https://kodi.wiki/view/Add-on_development)
- [Python API для Kodi](https://codedocs.xyz/xbmc/xbmc/)
- [JSON-RPC API для Kodi](https://kodi.wiki/view/JSON-RPC_API)

## Вопросы?

Если у вас есть вопросы или нужна помощь, пожалуйста:
1. Проверьте существующие плагины-службы для примеров
2. Изучите документацию Kodi
3. Откройте issue на GitHub

Успешной разработки! 🎉
